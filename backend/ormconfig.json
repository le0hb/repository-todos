{
    "type": "postgres",
    "host": "localhost",
    "port": 5432,
    "username": "postgres",
    "password": "docker",
    "database": "todos",
    "entities": [
        "./src/modules/**/models/typeorm/*.ts"
    ],
    "migrations": [
        "./src/database/typeorm/migrations/*.ts"
    ],
    "cli": {
        "migrationsDir": "./src/database/typeorm/migrations"
    }
}